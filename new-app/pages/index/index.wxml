<!--index.wxml-->
<view>
  <view class="userinfo" wx:if="{{!hasUserInfo}}">
    <button open-type="getUserInfo" bindgetuserinfo="getUserInfo">用户授权登录 </button>
  </view>
  <view class="userinfo" wx:else>loading...</view>
</view>
